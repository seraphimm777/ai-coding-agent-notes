# Copy this file to .env (or just `export` the variable in your shell) before running the agent.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the default model
# AGENT_MODEL=claude-sonnet-4-5
